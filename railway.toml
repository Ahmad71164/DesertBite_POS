[build]
builder = "NIXPACKS"
buildCommand = "npm ci --ignore-scripts && npm run build:shared && npm run build:api"

[deploy]
startCommand = "npm --workspace @restaurant-pro/api run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
